# 1. 先忽略所有文件和文件夹（* 匹配所有）
*

# 2. 排除 .gitignore 文件本身（否则这个配置文件会被忽略）
!.gitignore

# 3. 放行你需要推送的文件夹（以 ! 开头，末尾加 / 表示文件夹）
!data/
!papers/
!projects/
!runs/
!notes/
!bin/

# 4. 放行这些文件夹下的所有文件和子目录（关键！）
!data/**
!papers/**
!projects/**
!runs/**
!notes/**
!bin/**

# （可选）如果文件夹内有不想推送的子文件/子文件夹，可单独排除
# 比如：放行 folder1，但排除 folder1/temp 子文件夹
# folder1/temp/